#!/bin/sh

[ -f .git/description ] &&
    head -n 1 .git/description | grep -v "Unnamed repository; edit this file 'description' to name the repository." &&
        exit
[ -f .title ] &&
    head -n 1 .title
    exit
