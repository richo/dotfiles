[user]
	name = richo
	email = richo@psych0tik.net
	signingkey = 89E72415
[github]
    user = richoH
    token = [TOKEN CENSORED]
[core]
    excludesfile = ~/.gitignore
    whitespace=fix,-indent-with-non-tab,trailing-space
[color]
    branch = auto
    diff = auto
    status = auto
    interactive = auto
    grep = auto

[color "branch"]
    current = yellow reverse
    local = yellow
    remote = green

[color "diff"]
    meta = yellow bold
    frag = magenta bold
    old = red bold
    new = green bold
[color "status"]
    added = green
    changed = red
    untracked = cyan
[format]
    pretty = medium
[log]
    date = relative
[push]
    default = tracking
[rebase]
    stat = true
[alias]
    br = branch
    bra = branch -a
    ci = commit
    co = checkout
    cp = cherry-pick
    cs = commit -s
    dc = diff --cached
    detach = checkout HEAD^0
    df = diff
    fe = fetch
    ffo = merge --ff-only @{u}
    ffr = rebase origin/@{u}
    fmerge = merge --no-ff
    graph = log --pretty=format:\"%C(yellow)%h%C(red)%d %C(reset)%s %C(green)%an %C(bold black)%cr\" --graph --date=short
    g = grep
    lfo = log HEAD..FETCH_HEAD
    pall = pull --all
    pushup = "!f() { git branch | sed -e 's/* //'; }; git push --set-upstream origin `f`"
    rb = rebase origin/@{u}
    ri = rebase --interactive
    rmc = rm --cached
    rpull = pull --rebase
    sfo = show HEAD..FETCH_HEAD
    st = status
    wbranch = branch --contains
    wtag = name-rev --name-only
    edit-unmerged = "!f() { git ls-files --unmerged | cut -f2 | sort -u ; }; vim -p `f`"
[branch]
    autosetupmerge = true
[advice]
    statusHints = false
[diff]
	guitool = gvimdiff
[difftool]
	prompt = false
